/* import adminController from "../../src/controllers/admin/adminController.js";

//Test para obtener todos los trabajadores
describe("Test para obtener todos los trabajadores", () => {
    let adminController;
    beforeEach(() => {
        adminController = new adminController();
    });
    
    test("Test para obtener todos los trabajadores", async () => {
        const workers = await adminController.getAllWorkers();
        expect(workers).tobeTruthy();
    })
}) */